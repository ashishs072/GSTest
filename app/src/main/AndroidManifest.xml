<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gstest">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="false"
        android:icon="@drawable/nasa_splash"
        android:label="@string/app_name"
        android:roundIcon="@drawable/nasa_splash"
        android:supportsRtl="true"
        android:theme="@style/Theme.GSTest">
        <activity android:name=".ui.MainActivity"/>
        <activity android:name=".ui.SelectDateActivity"/>
        <activity android:name=".ui.PictureDetailActivity"/>
        <activity android:name=".ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>